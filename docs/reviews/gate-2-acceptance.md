# Gate 2 acceptance record

Base: accepted main `736d9047cc62b9b666521d01648f94e04fed995e` (PR #35). Scope: [issue #3](https://github.com/vivekpatel99/my-portfolio-webisite/issues/3). Work took place only in the isolated `e8f9` worktree on `codex/gate-2-evidence-safe-case-studies`.

## Evidence and publication boundary

`CONTEXT.md` and `docs/claims/gate-1-claim-ledger.json` remain authoritative. The three previously public stories are deepened using their accepted implementation scope. Design rationale is explicitly an interpretation of that scope; results are implementation observations, not measured client outcomes or acceptance.

No new client identity, delivery date, screenshot provenance, performance metric, testimonial, or related credential is approved by this change. No private source is published. Marketplace and general profile links are context, not independent verification of results. Repository proof remains unavailable where no approved public source exists.

The unreferenced football video, football image, planning graph, and pose image with unresolved provenance were removed from `public/` so Vite cannot distribute them. Their Gate 1 dispositions do not approve them as extraction or pose-delivery evidence. There are no approved video stories: no player, autoplay, loop, caption track, or playback controls are shipped. Static schematics have visible illustration captions and work with reduced motion. Future audiovisual media requires separate provenance and accessibility approval.

## Native routing

The native `collaboration.spawn_agent` calls accepted these explicit routes without substitution:

| Agent | Requested model | Requested effort | Responsibility |
| --- | --- | --- | --- |
| inventory | GPT-5.6 Luna | medium | Read-only consumer and browser matrix inventory |
| publishing | GPT-5.6 Terra | high | Publication schema, content, build and data tests |
| story_ui | GPT-5.6 Terra | high | Story UI, navigation, captions, fallbacks and component tests |
| review_standards | GPT-5.6 Sol | high | Fresh, read-only code/security/accessibility review |
| review_evidence | GPT-5.6 Sol | high | Fresh, read-only specification and evidence review |

Verified native session `turn_context` records report `gpt-5.6-luna` / `medium` for inventory (session `01a07631-a8a8-7b60-9928-1247f600d07b`), `gpt-5.6-terra` / `high` for publishing (`01a07632-03a6-7652-9f07-e9cfd5f92127`), and `gpt-5.6-terra` / `high` for story_ui (`01a07632-37bd-78e0-ae61-75170b544656`). Fresh review session records also confirm `gpt-5.6-sol` / `high`: standards `01a0763d-0d58-7190-bcf9-70fd7a3748c2` and evidence `01a0763d-4767-7bb1-9fb7-5883c0af2985`. The parent session reports `gpt-6-astra` / `high`. A subagent self-description inferred from generic prompt text was discarded in favor of these native records. Subagents were prohibited from GitHub writes and from using `.agents/sol-advisor`. The parent owns integration, verification, acceptance and PR state.

## Acceptance evidence

- Baseline: Node 24.20.0, 18 unit-test files / 122 tests passed before implementation.
- Pass 1: Node 24.20.0, 21 files / 144 unit tests passed, including schema negatives and real build-consumer withdrawal/zero-published fixtures.
- Pass 2: all 16 focused desktop/mobile browser checks passed, including axe WCAG A/AA checks, keyboard anchors, 320px reflow, metadata, and unavailable routes. Initial contrast failures were corrected and rerun.
- Pass 3: Node 24.20.0 full tests (144) and production build passed; local-only SEO passed; full passive Playwright passed 175 tests with 5 intentional skips in 1.1 minutes; `git diff --check` passed. Skips are the valid-contact submission guards and a desktop-only cursor assertion on mobile.
- Fresh independent review: pending completion below.
- The original checkout status was captured before work and compared byte-for-byte after implementation; unchanged. No task writes were made there.
- Screenshots: generated by `tests/qa/qa-case-studies.spec.js` for every published story on desktop and mobile; retained in the CI `passive-qa` artifact. Six additional local preview captures at 1280px and 390px are committed in `docs/reviews/gate-2-screenshots/`. These show this website preview, not client screenshots or delivery evidence.

## Manual acceptance

1. Open the local production preview homepage, follow each published card, and read all eight sections.
2. Use Tab and Enter on the story navigation; check Evidence and Limitations remain readable below the fixed header. Follow Case Studies, services and client-feedback navigation.
3. Inspect the 320px and 390px layouts and desktop layout, including captions and related-experience separation. Repeat with reduced motion.
4. Visit `/project/withheld-case-study/` and an unknown project slug directly and through client navigation. Both should render Page Not Found with `noindex, nofollow`; neither belongs in the sitemap or generated static routes.
5. Inspect source HTML metadata and the generated Apache project allowlist. The Vite preview uses an SPA fallback; deployment-server HTTP behavior must still be manually checked on Apache when deployment is separately authorized.
6. Confirm withheld evidence stays withheld. Do not submit valid contact data. Give manual acceptance on the draft PR after reviewing the screenshots and CI results.

This change stays draft and does not merge, deploy, close #3, or replace Vivek's manual acceptance.

## Repeatable local commands

Use Node 24 and a valid production-shaped Convex URL for the build. No contact data is submitted.

```sh
npm ci
npm test
VITE_CONVEX_URL=https://coordinated-mandrill-587.eu-west-1.convex.cloud npm run build
npm run preview -- --port 3102 --strictPort
QA_LOCAL_ONLY=1 QA_PREVIEW_URL=http://127.0.0.1:3102 npm run qa:seo
QA_LOCAL_ONLY=1 QA_PREVIEW_URL=http://127.0.0.1:3102 QA_PROD_URL=http://127.0.0.1:3102 npm run qa:playwright:ci -- --workers=2
git diff --check
```
