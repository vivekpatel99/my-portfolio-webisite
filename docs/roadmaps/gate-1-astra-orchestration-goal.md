# Gate 1 Astra Orchestration Goal

Paste the following into a new Codex task running `gpt-6-astra` with `xhigh` or higher reasoning.

```text
/goal Act as Astra orchestrator, architect, and final acceptor for Gate 1: https://github.com/vivekpatel99/my-portfolio-webisite/issues/12. Work from the latest `main` of `/Users/viv/Freelance/my-website/horizons-website` in an isolated worktree. Confirm `gpt-6-astra` with xhigh-or-higher reasoning and verify Git state, task tools, and worker-model availability; stop without substitution if routing is unobservable.

Make vivekapatel.com communicate one coherent, evidence-backed commercial position. Read `CONTEXT.md`, `docs/reviews/2026-09-05-astra-codebase-audit.md`, and the live website, then discover claim consumers and trustworthy evidence. Build a durable ledger classifying each public claim as verified, related credential, target/estimate, unsupported, or approval-required. Never invent evidence or present related work as a project result.

You are explicitly authorized to create and monitor three model-specific user-visible worker tasks. Use Luna (`gpt-5.6-luna`, max) for bounded read-only inventory and test mapping; Terra (`gpt-5.6-terra`, high) as sole implementation owner after Astra settles architecture; and fresh Sol (`gpt-5.6-sol`, high) for behaviorally read-only review of the final branch/diff. Give each a complete packet covering outcome, ownership, constraints, base, verification, Git boundary, and structured return. Serialize dependencies, inspect actual outputs, and return corrections to the same worker.

Implement coherent positioning across Hero, proof, services, About, testimonials, Case Study summaries, SEO, and tests. Resolve contradictory specialties, counts, scarcity, pricing, and ambiguous proof conservatively. Astra must inspect the full diff and rerun focused tests, full Vitest, production build, local SEO, and relevant passive Playwright desktop/mobile checks. Never submit valid forms, mutate production, edit marketplaces, merge, or deploy.

Require Sol verdict `ship`; any fix requires correction, complete Astra reverification, and fresh Sol review. Re-check PR state. After acceptance, commit and push Gate 1, then create a draft PR to `main`, or update the exact matching PR; include issue linkage, ledger, changed files, actual test results, screenshots, approval-required facts, risks, and manual test instructions. Do not mark ready or merge. An evidence-backed Gate 1 implementation, independently reviewed, fully verified, and delivered as an accurate draft PR from the latest accepted `main` is your /goal. Work completely autonomously and do not ask me for anything until you are all done.
```
